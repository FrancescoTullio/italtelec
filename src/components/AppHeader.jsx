import { Link } from "react-router-dom";

function AppHeader() {
    return (
        <header className="bg-dark text-white p-3">
            sono l'header
        </header>
    );
};

export default AppHeader;